import React from "react";

const Gallery = () => {
  const pageTitle = "Gallery"; // Set the title here

  return (
    <div className="container">
      <h1>{pageTitle}</h1>
      {/* Rest of your Gallery component */}
    </div>
  );
};

export default Gallery;
import React from "react";

const Blog = () => {
  const pageTitle = "Blog"; // Set the title here

  return (
    <div className="container">
      <h1>{pageTitle}</h1>
      {/* Rest of your Blog component */}
    </div>
  );
};

export default Blog;